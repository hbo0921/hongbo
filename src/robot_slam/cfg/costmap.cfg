#! /usr/bin/env python
PACKAGE = 'robot_slam'
from dynamic_reconfigure.parameter_generator_catkin import *
gen = ParameterGenerator()
gen.add("inflation_radius", 'double', 0, "Inflation radius for the costmap", 0.05, 0, 2.0)
exit(gen.generate(PACKAGE, "robot_slam", "MyStuff"))


#! /usr/bin/env python
PACKAGE = 'robot_slam'
from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

gen.add("inflation_radius", 'double', 0, "Inflation radius for the costmap", 0.2, 0, 0.3)

exit(gen.generate(PACKAGE, "robot_slam", "InflationRadiusConfig"))
